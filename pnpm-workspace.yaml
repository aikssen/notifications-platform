# Matches every directory under services/ that has a package.json.
# The Go services (dispatch-service, retry-service) are simply skipped.
packages:
  - 'services/*'

# pnpm 10 reads build-script approvals from here, not from package.json.
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
